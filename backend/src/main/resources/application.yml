spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/finora
    username: finora
    password: ***REMOVED***

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health

server:
  port: 8080