package com.finora.app.ui.theme

import androidx.compose.ui.graphics.Color

// Charcoal & Gold – Premium Finance Theme
// Inspired by Revolut Dark, American Express, and YNAB
val SpaceDark = Color(0xFF0F0F0F)   // True near-black charcoal (Apple-level dark)
val SurfaceDark = Color(0xFF1A1A1A) // Slightly lifted surface (cards, sheets)
val PrimaryNeon = Color(0xFFD4A843) // Warm Gold / Amber – timeless, premium, human
val SecondaryNeon = Color(0xFFE8C878) // Lighter champagne gold – secondary highlights
val TextPrimary = Color(0xFFF5F5F5) // Warm near-white (not stark, easier on eyes)
val TextSecondary = Color(0xFF9E9E9E) // Neutral mid-gray (no blue tint)
val ErrorRed = Color(0xFFE57373)    // Warm muted red
val SuccessGreen = Color(0xFF66BB6A) // Natural leaf green
